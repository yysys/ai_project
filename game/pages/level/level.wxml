<view class="container">
  <view class="top-bar">
    <view class="icon-btn" bindtap="goBack">
      <text>←</text>
    </view>
    <text class="title">{{currentChapter}}</text>
    <view class="placeholder"></view>
  </view>

  <view class="content">
    <view class="level-grid">
      <view 
        class="level-node {{item.unlocked ? 'unlocked' : 'locked'}} {{item.completed ? 'completed' : ''}}" 
        wx:for="{{levels}}" 
        wx:key="id"
        data-id="{{item.id}}"
        bindtap="selectLevel"
      >
        <text class="level-number">{{item.id}}</text>
        
        <view class="stars" wx:if="{{item.completed}}">
          <text class="star" wx:for="{{[1,2,3]}}" wx:for-item="star" wx:key="*this">
            {{star <= item.stars ? '★' : '☆'}}
          </text>
        </view>
        
        <text class="lock-icon" wx:if="{{!item.unlocked}}">🔒</text>
      </view>
    </view>
  </view>

  <view class="bottom-bar">
    <text class="stars-info">星级: {{totalStars}}/{{maxStars}}</text>
  </view>
</view>
