# 故障排除指南 - 拯救菜狗

## 🚨 问题：文件不存在 game.json

### ✅ 文件验证

根据文件系统检查，所有文件都存在于正确位置：

```
/Users/qinkuang.chen/traeProject/ai_project/game/
├── app.json              ✅ 存在
├── app.js                ✅ 存在
├── app.wxss              ✅ 存在
├── pages/                ✅ 存在
│   ├── index/           ✅ 存在
│   ├── level/           ✅ 存在
│   ├── game/            ✅ 存在
│   └── result/          ✅ 存在
├── utils/                ✅ 存在
├── tests/                ✅ 存在
└── 其他文档文件         ✅ 存在
```

### 🔍 可能的原因

#### 原因1：抖音开发者工具缓存问题

**症状**：
- 文件确实存在，但工具提示"文件不存在"
- 可能是工具的缓存导致

**解决方案**：
1. **清除抖音开发者工具缓存**
   - 关闭抖音开发者工具
   - 删除工具的缓存目录（通常在 ~/Library/ 或 ~/AppData/ 下）
   - 重新启动工具

2. **重新导入项目**
   - 完全删除项目
   - 重新导入

#### 原因2：导入路径错误

**症状**：
- 导入了错误的目录
- 可能导入了 `ai_project` 而不是 `ai_project/game`

**解决方案**：
1. **确认导入路径**
   - 必须导入：`/Users/qinkuang.chen/traeProject/ai_project/game`
   - 不要导入：`/Users/qinkuang.chen/traeProject/ai_project`

2. **选择正确的文件夹**
   - 在文件选择器中，找到 `game` 文件夹
   - 选择 `game` 文件夹，不要选择里面的子文件夹

#### 原因3：项目配置问题

**症状**：
- app.json 配置不兼容抖音小游戏
- 页面路径配置错误

**解决方案**：
1. **检查 app.json 配置**
   ```json
   {
     "pages": [
       "pages/index/index",
       "pages/level/level",
       "pages/game/game",
       "pages/result/result"
     ],
     "window": {
       "navigationBarBackgroundColor": "#56ab2f",
       "navigationBarTitleText": "拯救菜狗"
     }
   }
   ```

2. **确认页面路径正确**
   - 所有页面文件都在 `pages/` 目录下
   - 每个页面都有对应的 .js, .wxml, .wxss, .json 文件

## 🛠️ 详细的导入步骤

### 步骤1：清理环境

1. **关闭抖音开发者工具**
   - 完全退出应用

2. **清理缓存**（可选）
   ```bash
   # macOS
   rm -rf ~/Library/Caches/com.bytedance.miniapp*
   
   # Windows
   # 删除 %APPDATA%\Local\Temp\*
   ```

### 步骤2：重新导入项目

1. **启动抖音开发者工具**
   - 使用抖音账号登录

2. **点击"导入项目"**
   - 在文件选择器中，导航到：`/Users/qinkuang.chen/traeProject/ai_project`

3. **选择 game 文件夹**
   - **重要**：选择 `game` 文件夹，不要选择里面的子文件夹
   - 确认选择的路径是：`/Users/qinkuang.chen/traeProject/ai_project/game`

4. **填写项目信息**
   - 项目名称：拯救菜狗
   - AppID：填入你的抖音小游戏 AppID（如果没有，选择"测试号"）
   - 点击"导入"

### 步骤3：验证导入

1. **查看项目结构**
   - 在抖音开发者工具左侧，查看项目文件树
   - 确认能看到以下结构：
     ```
     game/
     ├── app.js
     ├── app.json
     ├── app.wxss
     ├── pages/
     │   ├── index/
     │   ├── level/
     │   ├── game/
     │   └── result/
     ├── utils/
     └── tests/
     ```

2. **查看编译日志**
   - 查看右侧的"编译"面板
   - 确认没有错误
   - 如果有错误，查看错误信息

## 🔧 手动检查清单

### 文件完整性检查

- [ ] app.json 存在于根目录
- [ ] app.js 存在于根目录
- [ ] app.wxss 存在于根目录
- [ ] pages/ 目录存在
- [ ] pages/index/ 目录存在
- [ ] pages/level/ 目录存在
- [ ] pages/game/ 目录存在
- [ ] pages/result/ 目录存在
- [ ] utils/ 目录存在
- [ ] 所有页面都有 .js, .wxml, .wxss, .json 文件

### 配置检查

- [ ] app.json 中的 pages 路径正确
- [ ] app.json 中的 window 配置正确
- [ ] sitemap.json 存在
- [ ] package.json 存在

## 🐛 常见错误信息

### 错误1："app.json not found"

**原因**：
- app.json 文件不在项目根目录
- 导入了错误的目录

**解决方案**：
1. 确认导入的是 `game` 文件夹
2. 不要导入 `game` 里面的子文件夹
3. 重新导入项目

### 错误2："Page is not found"

**原因**：
- 页面路径配置错误
- 页面文件不存在

**解决方案**：
1. 检查 app.json 中的 pages 配置
2. 确认所有页面文件都存在
3. 重新编译项目

### 错误3："Module not found"

**原因**：
- require 路径错误
- 模块文件不存在

**解决方案**：
1. 检查 require 路径是否正确
2. 确认模块文件存在
3. 检查相对路径是否正确

## 📞 获取帮助

### 如果以上方法都无法解决问题

1. **查看抖音官方文档**
   - https://partner.open-douyin.com/docs/resource/

2. **查看抖音开发者工具更新**
   - 确保使用最新版本
   - 旧版本可能有兼容性问题

3. **尝试使用微信开发者工具**
   - 下载：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
   - 导入相同项目测试
   - 如果在微信开发者工具中可以运行，说明代码没问题

4. **联系抖音技术支持**
   - 在抖音开放平台提交问题
   - 提供详细的错误信息和截图

## 🎯 快速验证步骤

### 验证文件存在

在终端中运行以下命令：

```bash
# 进入项目目录
cd /Users/qinkuang.chen/traeProject/ai_project/game

# 列出文件
ls -la

# 检查 app.json
cat app.json

# 检查 pages 目录
ls -la pages/
```

### 验证 JSON 格式

```bash
# 验证 app.json 格式
cat app.json | python -m json.tool
```

如果没有错误输出，说明 JSON 格式正确。

## 📊 当前项目状态

### 已确认的信息

✅ **app.json 文件存在**
- 位置：`/Users/qinkuang.chen/traeProject/ai_project/game/app.json`
- 大小：437 字节
- 格式：正确的 JSON

✅ **所有页面文件存在**
- pages/index/index.js, index.wxml, index.wxss, index.json
- pages/level/level.js, level.wxml, level.wxss, level.json
- pages/game/game.js, game.wxml, game.wxss, game.json
- pages/result/result.js, result.wxml, result.wxss, result.json

✅ **所有工具模块存在**
- utils/constants.js
- utils/unit.js
- utils/unitManager.js
- utils/levelManager.js
- utils/gameStateManager.js
- utils/gameEngine.js

✅ **所有测试文件存在**
- tests/constants.test.js
- tests/unit.test.js
- tests/unitManager.test.js
- tests/levelManager.test.js
- tests/gameStateManager.test.js

## 🎉 总结

所有文件都存在于正确的位置，app.json 配置也正确。如果抖音开发者工具仍然提示"文件不存在"，可能是：

1. **缓存问题** - 清除工具缓存
2. **导入路径问题** - 确保导入的是 `game` 文件夹
3. **工具版本问题** - 尝试更新到最新版本
4. **兼容性问题** - 尝试使用微信开发者工具测试

请按照上述步骤操作，如果还有问题，请告诉我具体的错误信息，我会继续帮你解决！🔧
